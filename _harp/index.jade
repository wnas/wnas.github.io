ul
	- public.articles._data.reverse();
	each article, slug in public.articles._data 
		if article.content_type != '2'
		li
			a(href="/articles/#{ article.slug }") #{ article.title } #{article.content_type}
